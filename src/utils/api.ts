import { mockAuthAPI, mockPdfAPI, mockHighlightAPI } from './mockApi';

// Use mock APIs for development
export const authAPI = mockAuthAPI;
export const pdfAPI = mockPdfAPI;
export const highlightAPI = mockHighlightAPI;